# 创作画布操作与恢复

本仓库是脱敏后的可复用模板，不绑定任何 Sites `project_id` 或站点域名。部署者应创建新的私有 Site，并由 Sites 在 `.openai/hosting.json` 中写入新项目 ID。

2026-09-15 画布改造：源于用户提供的 HuanyuSpellWeave 5.6.15 本地资源分析报告。报告未实测桌面 GUI，因此本站为独立实现，不宣称像素级复制。无 MCP、Railway 或电脑本地服务。

## 本轮新增与修改

- worker/canvas.js：图片档案、缩略图、画布乐观锁、公开 OAuth 配置与 Drive 归档元数据。
- worker/workbench.js：本地交付生成结果；保留旧历史；接入新端点。
- web/canvas.js、editor.js、storage.js、drive.js：画布、原尺寸编辑、原图恢复缓存与文件夹、可选 OAuth/Drive。
- web/app.js、index.html、style.css：三栏感的创作侧栏与自由画布，串行次数、历史档案、设置。
- db/schema.ts 和追加迁移 0001_huge_onslaught：assets/workspaces，不更改既有 jobs 迁移。
- scripts/build.mjs、test-workbench.mjs：前端打包与关键服务端回归。

## 现场排查

1. 生成未知：先查任务记录与供应商记录；不要自动再次提交。当前请求最长 240 秒，页面中断不保证交付。
2. 本机原图缺失：先找原下载文件；按 SHA-256 恢复。Drive 已归档则连接同一 Google 账号恢复。缩略图不能恢复原尺寸内容。
3. 缓存满：当前图仍保留内存并发起下载，串行生成停止；确认磁盘文件后再清理浏览器存储。
4. 画布冲突：先导出本页 JSON 再重新载入；没有自动导入布局功能，导出是人工恢复记录。
5. Google 无法连接：检查公开 Client ID、完整站点 origin、Drive API、同意屏幕和测试用户；绝不输入 Client Secret。
6. 上传失败：本地原图保留，重新点击先查已有文件。同页内保存可恢复上传会话；页面关闭后会话遗失，Google 的未完成会话按其规则到期。不会自动删除 Drive 文件。
7. 档案导出与布局导出均无 API Key/token，但会包含用户的提示词和参数。

## 发布前状态

构建、旧流程与新存储协议 workerd 模拟测试通过。无付费生图测试，无真实 Drive 授权测试。发布结果以 Sites 原生版本/部署记录为准。

2026-09-15 追加：参考图改为最多 14 张、编码后的整次请求 20 MB。列表可调整顺序，多选画布图可同时加入。Google 官方 Files API 未接到该中转站，因此超预算需用户减少或缩小参考图。新多图测试通过（模拟供应商，无扣费）。

追加：用户要求隐藏请求大小限制并自动优化参考图。web/prepare-references.js 在本机处理发送副本，目标完整请求 19.5 MB，质量优先并保留原文件。界面保留最多 14 张与自动优化提示。准备取消/失败不提交供应商；原图档案新增副本处理元数据。原生 @ 未获官方文档支持，按用户条件暂未添加。

追加：独立 Viewer + 3 路并发生图。本页最多 12 个活动/等待任务，每组最多 8 次。只有准备本组参考图短暂禁用表单，提交后立即释放。队列只在页面中运行，生成中保持页面打开。部署必须应用追加迁移 0002_previous_lyja 删除旧单任务索引。模拟并发与双击指针目标回归通过。


2026-09-15 Drive 上传报错排查：旧前端仅保留 HTTP 状态，丢失 Google error.reason/message；浏览器到 Google 的响应没有进入 Sites Worker 日志，无法据旧笼统提示确定用户的具体原因。新增 web/drive-errors.js 翻译与脱敏，记录阶段/status/reason/message 于页面内存；连接后自动只读检查 files.list，并提供手动检查按钮。403 配置/权限错误不再被上传进度查询覆盖，401 清除授权，失效上传会话清除；新会话直接提交文件，不先额外查询空会话。模拟测试 scripts/test-drive.mjs 覆盖 API 禁用、空间不足、401、真实 PUT 请求形状和远端内容校验。没有访问用户 Google token 或进行真实 Drive 上传。下一步由用户刷新并重新连接，获取完整错误信息，才能确定实际根因。

2026-09-15：用户要求 Drive 文件说明附带生成提示词。已更新创建上传会话 metadata.description；已有归档仅补写空白说明，不覆盖用户说明。模拟测试验证创建、补写、保留现有说明；未使用用户 Google 授权做真实上传。
